"use client";

export function showUnAvailableToast(toast) {
  toast({
    title: "Coming soon.",
    description: "This feature is not yet available.",
  });
}
export function showCopiedToast(toast) {
  toast({
    title: "Copied!",
  });
}
